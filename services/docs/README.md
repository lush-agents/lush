# Docs

Next.js documentation site for Lush, built with Fumadocs.

## Development

```sh
bun run --cwd services/docs dev
```

The local docs service listens on `http://localhost:7332`. The root `bun run dev`
target starts it alongside the API and app.
