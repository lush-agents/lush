# Configuration

Backend service for settings, admin controls, environments, connectors,
governance configuration, and provider/model setup. The main app renders the
settings surface while this service owns the authoritative configuration state.
