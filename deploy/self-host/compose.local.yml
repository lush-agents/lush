services:
  api:
    environment:
      LUSH_APP_ORIGIN: https://localhost
      LUSH_PUBLIC_APP_URL: https://localhost
      LUSH_EMAIL_DELIVERY: log
      LUSH_TRUSTED_PROXIES: ${LUSH_LOCAL_CADDY_IP:-172.31.251.254}/32

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile.local:/etc/caddy/Caddyfile:ro
      - caddy-local-data:/data
      - caddy-local-config:/config
    depends_on:
      web:
        condition: service_healthy
    networks:
      edge:
        ipv4_address: ${LUSH_LOCAL_CADDY_IP:-172.31.251.254}

volumes:
  caddy-local-data:
  caddy-local-config:
